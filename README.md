# React + Vite

This is a React project demonstrating a mock display page which controls the 
front end via Redux and REST.

This current version has been set up with a development environment.
all you have to do is clone, install, and run both the server and development mode.
Any production mode will be linked to Spring Boot.